<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- In-app update: запуск системного installer'а APK через ACTION_VIEW.
         С Android 8 для установки из не-Play источников требуется явное
         permission, и при первом запуске инспектор должен один раз разрешить
         «Установка из неизвестных источников» именно для нашего приложения
         в системных настройках (Android сам выкинет туда диалогом). -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <uses-feature
        android:name="android.hardware.camera.any"
        android:required="false" />

    <application
        android:name=".MatcheckApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Matcheckmobile">
        <!-- Sentry инициализируем вручную (initSentry в MatcheckApplication) с
             кастомным beforeSend-скраббингом → авто-инициализацию отключаем. -->
        <meta-data
            android:name="io.sentry.auto-init"
            android:value="false" />
        <!-- Просим Google Play services заранее подтянуть OCR-модуль для распознавания
             госномера. Гарантию даёт только установка из Play Store, а мы раздаём APK
             сайдлоадом, поэтому дополнительно зовём ModuleInstall.deferredInstall в
             AppContainer. Пока модуль не приехал, распознавания просто нет — ввод ручной. -->
        <meta-data
            android:name="com.google.mlkit.vision.DEPENDENCIES"
            android:value="ocr" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|keyboardHidden"
            android:resizeableActivity="false"
            android:screenOrientation="nosensor"
            android:theme="@style/Theme.Matcheckmobile"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
