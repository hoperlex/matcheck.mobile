*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# Reference web project extracted from ZIP
/docs/reference/*.zip
/.idea/

# MCP-config для локального read-only Postgres из портала.
# Ссылается на абсолютные пути этой машины (/root/projects/matcheck/...),
# в репе для команды не годится.
/.mcp.json

# Служебный каталог Kotlin 2.x: логи компилятора и .salive-сессии демона.
# Локальный мусор сборки, генерируется заново на каждой машине.
/.kotlin/

# Дампы падения JVM (Android Studio / Gradle) — локальный мусор, PID в имени меняется.
hs_err_pid*.log
replay_pid*.log
# Разовый экспорт-архив приложения.
app.zip